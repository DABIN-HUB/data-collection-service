# 全面检查与测试计划

本文档记录本轮对 `data-collection-service` 的全面检查方案。目标是覆盖主要采集协议、采集调度、数据处理、缓存、上报、配置治理、设备生命周期和自适应采集能力，形成可复现的测试基线和问题清单。

## 1. 检查目标

1. 确认设备启动、停止、重载、配置刷新等生命周期操作不会产生旧任务继续写缓存、上报、实时流或历史库的问题。
2. 确认主要采集协议的读取链路从配置解析、连接建立、协议读取、值转换、质量判定到 `ProcessResult` 输出是闭环的。
3. 确认缓存、Redis Stream、历史写入和上报链路在成功、失败、限流、重试、下游不可用时行为可控。
4. 确认采集频率、批大小和时间片等自适应逻辑不会突破协议限制，不会导致线程池、队列或内存持续膨胀。
5. 建立一套可持续执行的回归测试矩阵，区分已有失败、缺失覆盖和新发现问题。

## 2. 检查范围

### 2.1 采集协议

重点检查以下协议的采集链路：

1. `MODBUS_TCP` / `MODBUS_RTU`
2. `MQTT`
3. `SNMP`
4. `HTTP`
5. `WEBSOCKET`
6. `COAP`
7. `OPC_UA` / `OPC_DA`
8. `IEC104` / `IEC61850`
9. `BACNET_IP`
10. `CUSTOM_TCP`

说明：`CUSTOM_TCP` 当前按占位实现检查，验收标准是“不误报为可真实采集”，不按真实协议采集能力验收。

### 2.2 主链路模块

1. `CollectionService`：启动入口和服务层行为。
2. `CollectionScheduler`：设备启动、停止、重载、时间片、批次调度、超时、重连、代际保护。
3. `CollectionManager`：采集器注册、连接、读写分发、读计划重建。
4. `CollectorFactory`：协议采集器创建和协议枚举覆盖。
5. `BaseCollector`：统一读取、数据转换、质量处理、`ProcessResult` 生成。
6. `CollectorDataPostProcessor` / `CollectorDataCacheAspect`：异步后处理、缓存、上报、实时流、历史写入入口。
7. `MultiLevelCacheManager`：本地缓存、Redis 缓存、多级读写一致性。
8. `CacheReportService` / `ReportManager`：设备影子、聚合、限流、重试、上报处理。
9. `ConfigManager` / `ConfigController`：设备、连接、点位配置读取和在线治理。
10. `ThreadPoolConfig`：采集、调度、后处理、上报、心跳相关线程池。

## 3. 测试矩阵

### 3.1 生命周期测试

1. 单设备正常启动、重复启动、启动失败回滚。
2. 单设备停止后，在途采集结果不得继续进入缓存、上报、实时流、历史写入。
3. 设备重载后，旧代任务和旧代后处理结果必须被丢弃。
4. 全量启动、全量停止、批量设备部分失败。
5. 连接失败、连接超时、断线重连、重连失败退避。

### 3.2 协议链路测试

1. 协议字段配置覆盖：按 `docs/protocols/FIELD_CONFIG_SUMMARY.md` 和真实代码读取字段比对。
2. 连接适配器测试：连接、断开、心跳、重连、异常传播。
3. 单点读取测试：正常值、空值、异常值、类型不匹配。
4. 批量读取测试：全部成功、部分失败、协议上限切批、地址跨度切批。
5. 模拟服务端集成测试：优先覆盖可本地模拟的 Modbus TCP、HTTP、WebSocket、MQTT、SNMP。

### 3.3 数据处理测试

1. 原始值到目标类型转换。
2. 缩放、偏移、布尔/数值转换。
3. 质量判定：GOOD、WARNING、BAD、TIMEOUT、CONNECTION_LOST。
4. `ProcessResult` 字段完整性：原始值、处理值、最终值、质量、时间戳、消息、元数据。
5. 批量结果中单点失败不影响其他点位处理。

### 3.4 缓存与实时流测试

1. 本地缓存写入、读取、过期、删除。
2. Redis 缓存写入失败时主链路不被中断。
3. 多级缓存同 key 并发读写一致性。
4. Redis Stream 成功写入、禁用配置、写入失败降级。
5. 历史写入启用/禁用、写入失败降级。

### 3.5 上报测试

1. 点位变化进入设备影子，flush 后只提交脏数据。
2. 单 chunk 成功后标记已上报。
3. 部分 chunk 失败时设备窗口不得错误提交。
4. `retryable=true` 按重试上限退避重试。
5. `deferred=true` 按重试上限处理，不得无限排任务。
6. 上报线程池拒绝、网关限流、目标不可用时行为可控。

### 3.6 配置治理测试

1. 查询配置摘要、设备详情、点位、连接、差异。
2. 在线更新设备配置、连接配置、点位配置。
3. 刷新单设备配置时采集任务重建。
4. 启动中修改配置、采集中修改配置、停止中修改配置。
5. 无效配置、缺字段、协议不支持时返回清晰错误。

### 3.7 自适应与压力测试

1. 批大小根据耗时调整，但不得超过协议批次上限。
2. 时间片数量和间隔调整后，旧 revision 任务不得继续执行。
3. 大量设备、大量点位、慢设备、离线设备混合场景。
4. Redis 慢/断、上报慢/断、网络抖动下线程池队列不得无界增长。
5. 长时间运行后检查内存、任务数、缓存锁、重试任务、设备影子数量。

## 4. 执行顺序

### 阶段 1：基线建立

1. 运行全量单元测试：`mvn test`。
2. 运行已有 P0 回归套件。
3. 记录当前失败项，标记为已有失败、环境依赖失败、代码回归失败。
4. 生成初始问题清单。

### 阶段 2：静态链路检查

1. 按主链路入口逐个阅读核心类。
2. 对照文档和代码列出协议配置字段实际读取情况。
3. 检查线程池、队列、锁、异步任务、重试任务的边界。
4. 检查异常是否会吞掉、无限重试、阻塞主链路或错误提交状态。

### 阶段 3：补充自动化测试

1. 优先补生命周期、代际保护、缓存并发、上报重试这类高风险回归测试。
2. 为可模拟协议补本地集成测试。
3. 为难以模拟协议补采集器和配置解析单元测试。
4. 对配置治理接口补 MockMvc 或服务层测试。

### 阶段 4：故障注入与压力测试

1. 下游 Redis 不可用。
2. 上报目标不可用。
3. 设备离线或心跳超时。
4. 采集线程池、后处理线程池、上报线程池满载。
5. 大设备数和大点位数场景下观察队列、内存、任务释放。

### 阶段 5：输出报告

1. 输出问题清单：风险等级、位置、复现方式、影响范围、修复建议。
2. 输出测试结果：已通过、失败、跳过、依赖真实环境的项目。
3. 输出后续建议：必须修复项、建议修复项、需要真实设备联调项。

## 5. 验收标准

1. 全量测试结果有明确基线，失败项能解释原因并关联到具体模块。
2. 高风险链路至少有自动化回归测试覆盖。
3. 启动、停止、重载和配置刷新不会出现旧任务继续写结果。
4. 下游故障不会把采集线程拖死，不会产生无限任务或无界队列。
5. 主要协议的配置字段和采集路径有测试或明确检查结论。
6. 上报失败、部分成功、限流、重试耗尽时设备影子状态符合预期。

## 6. 当前第一轮测试命令

本轮先执行以下命令建立基线：

```bash
mvn test
```

如全量测试耗时过长或存在环境依赖失败，再分组执行：

```bash
mvn -q "-Dtest=CollectorDataPostProcessorTest,CacheReportServiceTest,MultiLevelCacheManagerTest,CollectionSchedulerTest" test
mvn -q "-Dtest=*Config*,*Controller*" test
mvn -q "-Dtest=*Collector*,*Scheduler*,*Manager*" test
```

## 7. 输出物

1. `COMPREHENSIVE_TEST_PLAN.md`：本计划文档。
2. `COMPREHENSIVE_TEST_REPORT.md`：测试执行和问题报告，测试完成后生成。
3. 必要的回归测试代码：按发现的问题补充到 `src/test/java`。
