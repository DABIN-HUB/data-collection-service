# data-collection-service 工程完整功能与实现逻辑

## 1. 工程定位与总体架构

本工程是一个基于 Spring Boot 的工业/设备数据采集服务，核心目标是：

1. 按设备和点位配置进行周期采集。
2. 对采集值做标准化处理、质量判定、告警触发。
3. 将结果写入多级缓存，并按策略进行实时上报。
4. 输出系统健康、性能、异常等监控指标。
5. 提供 HTTP 管理接口用于运行控制和观测。

整体分层：

- `api`：REST 接口层（设备控制、数据查询、监控指标、健康检查）。
- `core`：采集调度、连接管理、协议采集器、处理链、缓存切面。
- `common`：通用模型、配置、Redis/线程池等基础能力。
- `monitor`：健康与性能监控聚合。
- `storage`：持久化相关接口（当前多数为占位实现）。

启动入口：`src/main/java/com/wangbin/collector/Application.java`

- 启用 `@EnableAsync`：异步执行采集、上报等任务。
- 启用 `@EnableScheduling`：调度采集任务、定时裁剪等。
- 启用 `@EnableAspectJAutoProxy`：缓存切面拦截采集器读方法。

---

## 2. 核心采集功能

### 2.1 采集入口与生命周期

关键类：

- `core/collector/CollectionService`：对外统一入口（start/stop/reload/status）。
- `core/collector/scheduler/CollectionScheduler`：实际调度核心。

主要能力：

1. 启动全量设备采集任务。
2. 停止指定设备或全局采集。
3. 动态重载设备配置并刷新任务。
4. 维护每个设备运行状态、失败次数、采集统计。

### 2.2 任务调度与并发执行

关键类：

- `CollectionScheduler`
- `DeviceBatchPlanner`
- `DeviceBatchTask`
- `CollectedDataProcessor`
- `AdaptiveCollectionUtil`

实现逻辑：

1. 基于设备采集周期构建时间片任务。
2. 通过批量规划器将设备分批执行，避免单次高峰。
3. 在采集线程池异步读取点位，再交给处理线程池。
4. 根据执行耗时和负载做自适应频率调整（配置开启时）。

### 2.3 协议采集器体系

关键类：

- `core/collector/CollectorFactory`
- `core/collector/CollectionManager`
- `core/collector/protocol/ProtocolCollector`（接口）
- `core/collector/protocol/BaseCollector`（模板基类）

实现逻辑：

1. `CollectorFactory` 按协议类型创建采集器实例（Modbus/OPC/IEC/MQTT/CoAP/SNMP 等）。
2. `CollectionManager` 管理采集器注册、启动、停止、读写调用。
3. `BaseCollector` 内统一实现连接检查、异常处理、质量处理、统计记录。

---

## 3. 数据处理与质量控制

### 3.1 处理结果模型

关键类：

- `core/collector/processor/ProcessResult`

作用：

- 作为采集后统一结果对象，包含值、质量、时间戳、原始值、元数据、告警/状态信息。
- 后续缓存、实时流、上报都围绕该对象展开。

### 3.2 处理器与处理链

关键类：

- `DataProcessorManager`
- `ProcessorChainManager`
- `DataProcessorChain`
- `QualityFilter`
- `DeadbandFilter`
- `DataQualityProcessor`

实现逻辑：

1. 采集原始值进入处理器链。
2. 执行质量判定、死区过滤、业务规则校验。
3. 生成标准 `ProcessResult`。
4. 若触发规则，由 `AlertManager` 上报告警。

---

## 4. 缓存与实时流能力

### 4.1 多级缓存

关键类：

- `core/cache/MultiLevelCacheManager`
- `core/cache/LocalCacheManager`
- `core/cache/RedisCacheManager`

实现逻辑：

1. 支持本地缓存 + Redis 缓存。
2. 支持 `read-through`、`write-through` 等策略。
3. 提供缓存命中率、容量、健康统计。

### 4.2 采集缓存切面（不改原采集器业务）

关键类：

- `core/cache/aspect/CollectorDataCacheAspect`

实现逻辑：

1. AOP 拦截 `ProtocolCollector.readPoint/readPoints` 返回结果。
2. 异步将结果写入缓存。
3. 同步触发上报聚合服务。
4. 追加写入 Redis Stream（实时流）。

### 4.3 Redis Stream 实时数据

关键类：

- `core/cache/config/TelemetryStreamProperties`
- `core/cache/enum/StreamRetentionMode`
- `core/cache/service/TelemetryStreamService`
- `core/cache/service/impl/TelemetryStreamServiceImpl`
- `core/cache/util/TelemetryStreamRecordBuilder`

支持能力：

1. 基于 `ProcessResult` 完整字段写入 Redis Stream。
2. `COUNT` 模式：通过 `XADD MAXLEN` 保留最近 N 条。
3. `TIME` 模式：消息带 `eventTs`，并通过定时 `XTRIM MINID` 保留最近 N 秒。
4. `approximate-trim` 控制近似裁剪。
5. 提供开关控制启停，且不影响原采集流程。

TIME 模式实现说明：

- Redis Stream 原生更适合按 ID 范围裁剪，不直接按“秒窗口字段”自动过期。
- 本工程方案是：
  1. 写入时将事件时间映射到 Stream ID 时间维度；
  2. 定时任务计算阈值 ID；
  3. 执行 `XTRIM MINID` 删除阈值之前数据。
- 复杂度：每次裁剪约 `O(logN)` 到 `O(N)`（取决于内部结构和删除量）。
- 风险：
  1. 高并发下近似裁剪会出现短时间“略多于窗口”的数据量；
  2. 事件时间与服务时钟偏差过大时，窗口边界会有漂移。

---

## 5. 上报系统

关键类：

- `core/report/CacheReportService`
- `core/report/ReportManager`
- `core/report/handler/*`（HTTP/MQTT/TCP）
- `core/report/shadow/ShadowManager`
- `core/report/shadow/DeviceShadow`

实现逻辑：

1. 采集缓存结果先进入 `CacheReportService`。
2. 更新设备影子，仅将变化字段标记为 dirty。
3. 定时批量 flush 脏影子，按上报配置聚合发送。
4. `ReportManager` 根据 handler 类型分发到对应通道。
5. 支持异步、限流、重试、分片、事件上报与告警上报。

---

## 6. 连接管理

关键类：

- `core/collector/connection/ConnectionManager`
- `core/collector/connection/ConnectionFactory`
- `core/collector/connection/adapter/*`
- `core/collector/connection/ConnectionMetrics`

实现逻辑：

1. 为不同协议创建连接适配器。
2. 管理连接池、心跳探测、自动重连、状态切换。
3. 输出连接性能和可用性指标，供监控与调度策略使用。

---

## 7. 监控与可观测

### 7.1 健康聚合

关键类：

- `monitor/SystemHealthService`

聚合维度：

- 采集器状态
- 缓存状态
- 连接状态
- 应用基础状态

### 7.2 监控指标

关键类：

- `monitor/metrics/CacheMonitorService`
- `monitor/metrics/DeviceMonitorService`
- `monitor/metrics/PerformanceMonitorService`
- `monitor/metrics/ExceptionMonitorService`

作用：

- 提供缓存命中、设备在线、采集耗时、异常分布等指标。

### 7.3 对外接口

关键控制器：

- `api/controller/DeviceController`
- `api/controller/DataController`
- `api/controller/MonitorController`
- `api/controller/HealthController`
- `api/controller/CacheController`

---

## 8. Redis 与线程池基础配置

### 8.1 Redis

关键类：`common/config/RedisConfig`

提供：

- 主 `redisTemplate`
- `cacheRedisTemplate`
- `stringValueRedisTemplate`
- `StringRedisTemplate`

特点：

- 已统一序列化风格，可被缓存与实时流直接复用。

### 8.2 线程池

关键类：`common/config/ThreadPoolConfig`

提供采集、处理、上报、监控等独立线程池，避免互相阻塞。

---

## 9. 配置体系与关键配置项

当前主要配置在 `src/main/resources/application.yml`，包含：

- `collector.*`：采集、调度、连接、重试、处理器相关。
- `collector.report.*`：上报开关、模式、周期、MQTT 等。
- `spring.data.redis.*`：Redis 连接池与超时。
- `monitor.*`：监控策略。

Redis Stream 相关建议配置示例：

```yaml
telemetry:
  stream:
    enabled: true
    key: collector:telemetry:stream
    retention-mode: COUNT   # COUNT 或 TIME
    max-length: 200
    max-seconds: 60
    approximate-trim: true
    trim:
      enabled: true
      interval-ms: 1000
```

说明：

- `COUNT` 用于固定条数窗口。
- `TIME` 用于固定时间窗口，需确保时钟同步。
- 建议将 `telemetry.stream` 放在根级，确保与 `@ConfigurationProperties(prefix="telemetry.stream")` 一致。

---

## 10. 当前未完成模块边界

以下模块当前为占位实现（stub）或功能未完全落地：

- `storage/service/HistoryDataService`
- `storage/service/TimeSeriesService`
- `storage/repository/DataRepository`
- `storage/repository/DeviceRepository`
- `storage/config/StorageConfig`
- `api/controller/ConfigController`
- `api/filter/LogFilter`

新增安全能力（2026-04-03）：

1. `api/filter/AuthFilter` 已实现运维 Token + 云端服务签名 + IP 白名单回退，并默认拦截 `/api/**`。
2. 配置入口 `collector.auth.*`，详见《09-访问鉴权与服务令牌》。
3. 通过鉴权的身份会以 `AuthFilter.ATTR_PRINCIPAL` 形式写入请求属性，供访问日志与后续链路复用。

这些模块不影响现有“实时采集 -> 处理 -> 缓存 -> 上报/实时流”的主链路，但后续若做持久化与平台化治理，需要优先补齐。
---

## 11. 关键调用时序（文字版）

### 11.1 单点采集主链路

1. 调度器触发设备点位读取。
2. 协议采集器读取并输出原始值。
3. 处理链生成 `ProcessResult`。
4. AOP 切面拦截结果并：
   - 写多级缓存
   - 调 `CacheReportService` 聚合上报
   - 调 `TelemetryStreamService` 写 Redis Stream

### 11.2 告警链路

1. 质量处理器检测到越界/异常。
2. `AlertManager` 生成告警事件。
3. `CacheReportService.reportAlert` 将告警推送到上报通道。

### 11.3 TIME 裁剪链路

1. 定时任务读取当前时间并计算阈值毫秒。
2. 生成阈值 Stream ID。
3. 执行 `XTRIM MINID`。
4. 保留窗口内消息。

---

## 12. 测试与验证建议

### 12.1 单元测试

- 已有实时流单测：`core/cache/service/impl/TelemetryStreamServiceImplTest`
- 建议继续补充：
  1. COUNT 模式 `MAXLEN` 参数验证。
  2. TIME 模式 `MINID` 阈值与裁剪调度验证。
  3. 切面在异常场景下不影响采集主流程验证。

### 12.2 集成验证步骤

1. 启动 Redis 与应用。
2. 开启采集任务并产生实时数据。
3. 使用 `XREAD`/`XRANGE` 观察流：
   - COUNT：消息总数稳定在 `max-length` 附近。
   - TIME：消息时间窗口稳定在 `max-seconds` 附近。
4. 关闭 `telemetry.stream.enabled`，确认采集与上报仍正常。

---

## 13. 后续演进建议

1. 将 `storage` 层从 stub 补齐为真实时序库存储。
2. 为 Stream 消费端补充消费组、重放、告警联动。
3. 增加端到端压测，验证高并发下裁剪成本和上报延迟。
4. 引入统一 tracing（采集任务 ID / 设备 ID / 点位 ID）增强故障定位。
