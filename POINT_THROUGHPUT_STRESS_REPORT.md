# 点位采集吞吐压力测试报告

## 1. 测试日期

2026-07-03

## 2. 测试目标

验证当前采集调度框架在不依赖真实设备的情况下，单轮采集周期能承载的点位规模。

本轮重点测的是：

1. 点位切批和时间片分配。
2. 批次任务分发。
3. 模拟协议读取。
4. 采集结果 Map 构建。
5. 数据处理线程池提交和处理。

本轮不包含：

1. 真实 PLC/设备网络耗时。
2. Redis 写入。
3. Redis Stream 写入。
4. 历史库写入。
5. MQTT/HTTP/TCP 上报目标 I/O。

因此本报告中的“无延迟”结果是框架侧上限，不是现场真实设备容量。

## 3. 测试入口

新增压测用例：

```text
src/test/java/com/wangbin/collector/core/collector/scheduler/CollectionThroughputStressIT.java
```

该用例命名为 `*IT`，普通 `mvn test` 不会自动执行。需要手动指定：

```bash
mvn -q "-Dtest=CollectionThroughputStressIT" test
```

可配置参数：

```bash
-Dcollector.stress.pointCounts=100000,500000
-Dcollector.stress.readDelayMs=5
-Dcollector.stress.sliceCount=16
-Dcollector.stress.batchThreads=16
-Dcollector.stress.collectThreads=64
-Dcollector.stress.processThreads=16
```

测试输出：

```text
target/stress/collection-throughput.csv
target/stress/collection-throughput-report.md
```

## 4. 测试配置

本轮主要配置：

1. 协议：`MODBUS_TCP`
2. 批次规划：约 `100` 点/批
3. 时间片数量：`16`
4. 批次分发线程：`16`
5. 模拟采集线程：`64`
6. 数据处理线程：`16`
7. 设备数量：`1`
8. 点位地址：连续地址
9. 自适应采集：关闭
10. 下游缓存和上报：不参与

## 5. 无协议延迟压测结果

命令：

```bash
mvn -q "-Dtest=CollectionThroughputStressIT" "-Dcollector.stress.pointCounts=1000,5000,10000,20000,50000,100000,200000,500000,1000000,2000000,3000000,5000000" test
```

结果：

| 点位数 | 批次数 | 启动/规划耗时 ms | 单轮处理耗时 ms | 已处理点位 | 吞吐 points/s |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 1,000 | 10 | 37 | 21 | 1,000 | 47,619 |
| 5,000 | 50 | 14 | 27 | 5,000 | 185,185 |
| 10,000 | 100 | 27 | 46 | 10,000 | 217,391 |
| 20,000 | 200 | 31 | 22 | 20,000 | 909,091 |
| 50,000 | 500 | 77 | 38 | 50,000 | 1,315,789 |
| 100,000 | 1,000 | 104 | 56 | 100,000 | 1,785,714 |
| 200,000 | 2,000 | 203 | 62 | 200,000 | 3,225,806 |
| 500,000 | 5,000 | 779 | 71 | 500,000 | 7,042,254 |
| 1,000,000 | 10,000 | 1,223 | 119 | 1,000,000 | 8,403,361 |
| 2,000,000 | 20,000 | 2,197 | 188 | 2,000,000 | 10,638,298 |
| 3,000,000 | 30,000 | 3,719 | 230 | 3,000,000 | 13,043,478 |
| 5,000,000 | 50,000 | 4,810 | 368 | 5,000,000 | 13,586,957 |

结论：

1. 在纯内存模拟读取场景下，当前框架至少能完成 `500 万` 点位单轮采集处理。
2. `500 万` 点位约拆成 `50,000` 个批次，单轮处理耗时约 `368ms`。
3. 大点位启动/规划耗时明显上升，`500 万` 点位启动和批次规划约 `4.8s`。
4. 该结果代表框架上限，真实设备场景会被协议请求耗时和下游 I/O 显著拉低。

## 6. 模拟 5ms 批次读延迟压测结果

命令：

```bash
mvn -q "-Dtest=CollectionThroughputStressIT" "-Dcollector.stress.pointCounts=100000,500000,1000000,2000000" "-Dcollector.stress.readDelayMs=5" test
mvn -q "-Dtest=CollectionThroughputStressIT" "-Dcollector.stress.pointCounts=150000,200000,300000" "-Dcollector.stress.readDelayMs=5" test
mvn -q "-Dtest=CollectionThroughputStressIT" "-Dcollector.stress.pointCounts=250000" "-Dcollector.stress.readDelayMs=5" test
```

结果：

| 点位数 | 批次数 | 模拟读延迟 | 启动/规划耗时 ms | 单轮处理耗时 ms | 已处理点位 | 吞吐 points/s |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 100,000 | 1,000 | 5ms/批 | 212 | 922 | 100,000 | 108,460 |
| 150,000 | 1,500 | 5ms/批 | 247 | 1,173 | 150,000 | 127,877 |
| 200,000 | 2,000 | 5ms/批 | 293 | 866 | 200,000 | 230,947 |
| 250,000 | 2,500 | 5ms/批 | 895 | 1,145 | 250,000 | 218,341 |
| 300,000 | 3,000 | 5ms/批 | 257 | 1,221 | 300,000 | 245,700 |
| 500,000 | 5,000 | 5ms/批 | 561 | 2,051 | 500,000 | 243,784 |
| 1,000,000 | 10,000 | 5ms/批 | 1,171 | 3,961 | 1,000,000 | 252,462 |
| 2,000,000 | 20,000 | 5ms/批 | 1,934 | 7,895 | 2,000,000 | 253,325 |

结论：

1. 模拟 `5ms/批` 的协议读耗时后，容量从框架上限转为读耗时主导。
2. 以 `1 秒` 单轮采集周期为目标，本机模拟结果显示可承载区间大约在 `20 万点` 左右。
3. `25 万点` 已经超过 `1 秒`，`50 万点` 进入 `2 秒` 级。
4. 由于 JVM 预热和系统调度存在波动，`15 万`、`20 万`、`25 万` 的单次结果不是严格单调，建议后续做多轮取 P50/P95。

## 7. 当前结论

按本机合成压测结果：

1. 框架侧无外部 I/O 的理论承载：至少 `500 万点/轮`。
2. 模拟 `5ms/批` 协议读取时，`1 秒周期` 建议按 `20 万点/轮` 以内评估。
3. 如果真实协议平均批次耗时高于 `5ms`，容量会继续下降。
4. 大规模点位主要风险不是线程池拒绝，而是启动/规划耗时、内存占用、真实协议 I/O 和下游 Redis/上报写入。

## 8. 现场容量估算方式

对真实设备，建议用以下方式估算：

```text
单设备点位吞吐 ~= 每批点位数 * 每秒可完成批次数
每秒可完成批次数 ~= 并发批次数 * 1000 / 平均单批协议耗时ms
```

示例：

1. Modbus 每批 `100` 点，平均单批 `5ms`，有效并发 `1`，约 `20,000 点/s`。
2. Modbus 每批 `100` 点，平均单批 `20ms`，有效并发 `1`，约 `5,000 点/s`。
3. 如果同设备连接层不能安全并发，实际容量应按单连接串行估算。

## 9. 后续建议

1. 用真实或 mock 协议服务端跑 Modbus TCP 真实 I/O 压测。
2. 增加 Redis、Redis Stream、上报链路参与后的端到端压测。
3. 对 `CollectionThroughputStressIT` 增加多轮 P50/P95 输出，减少单次 JVM 预热波动。
4. 对大设备数场景单独压测，例如 `1000 台设备 * 1000 点`，验证跨设备调度和连接管理压力。
