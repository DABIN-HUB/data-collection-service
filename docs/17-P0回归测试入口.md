# P0 回归测试入口

## 1. 目标

固定当前 P0 改造后的最小回归入口，避免每次手工拼接 `-Dtest=...` 参数。

## 2. 固定 Suite

测试套件：

- `src/test/java/com/wangbin/collector/regression/P0RegressionSuite.java`

当前纳入的测试类：

1. `CollectorDataPostProcessorTest`
2. `ModbusTcpCollectionChainAcceptanceTest`
3. `CollectionSchedulerTest`
4. `CacheReportServiceTest`
5. `TelemetryStreamServiceImplTest`
6. `ShadowManagerTest`
7. `CloudOutboxServiceTest`
8. `AdaptiveCollectionUtilTest`
9. `AlarmStateTrackerTest`
10. `RedisAlarmStateRepositoryTest`
11. `HistoryWriteBufferTest`

## 3. 固定执行方式

Maven profile：

- `p0-regression`

执行命令：

```bash
mvn -q -Pp0-regression test
```

## 4. 维护规则

1. 新增 P0 定向测试后，同步加入 `P0RegressionSuite`。
2. 如果某个测试上升为主链路关键回归，优先纳入该 suite，而不是继续堆叠命令行 `-Dtest`。
3. 默认 `mvn test` 保持全量测试行为，`p0-regression` 只负责固定的 P0 回归入口。

## 5. 2026-07-14 新增 P0 回归计划

完成《16-P0_P1实施任务单与改造方案》第 9 章任务时，Suite 至少新增以下场景：

1. 配置中心超时和非法响应时保留最后有效快照。
2. 远端明确空快照时才执行设备删除。
3. 单设备超时后下一周期继续执行。
4. 慢设备不影响同时间片其他设备。
5. 服务重启后 dirty 影子和 Outbox 可恢复。
6. ACK 丢失、重复和乱序按消息 ID 与版本幂等。
7. 伪造代理头、篡改请求体和 nonce 重放均被拒绝。
8. P0 回归必须同时验证协议采集数量、点位值、质量和请求次数未发生非预期变化。

新测试落地前本节表示计划，不得将对应 P0 风险标记为完成。

## 6. 2026-07-15 回归结果

1. 全量命令：`mvn test`。
2. 结果：`446` 项通过，失败 `0`、错误 `0`、跳过 `0`。
3. 前端语法：`node --check src/main/resources/static/admin/app.js` 通过。
4. 新增覆盖：配置失败保留快照、时间片超时恢复、请求防重放、生产秘密校验、连续采集快照隔离、告警 ACK 与恢复关联、配置原子回滚、敏感字段脱敏、Outbox 固定身份与 ACK 幂等、三种缓存模式装配。
5. 实际 Redis、TDengine、配置中心和云平台断网注入仍需在集成环境执行，不由单元测试结果替代。

## 7. 2026-07-15 第三轮回归结果

1. 全量命令：`mvn -o test`。
2. 结果：`449` 项通过，失败 `0`、错误 `0`、跳过 `0`。
3. P0 固定入口：`mvn -o -q -Pp0-regression test` 通过。
4. 新增覆盖：多设备聚合 Outbox 逐设备提交、告警状态重启恢复、TDengine 失败缓冲与回放。
5. 工程检查：前端脚本语法、UTF-8 严格解码和 Git 差异格式检查通过。

## 8. 2026-07-15 第四轮回归结果

1. 全量命令：`mvn -o test`。
2. 结果：`455` 项通过，失败 `0`、错误 `0`、跳过 `0`。
3. P0 固定入口：`mvn -o -Pp0-regression test`，`46` 项通过。
4. 新增覆盖：点位运行态与配置分离、历史元数据读取实际运行间隔、只读令牌越权拒绝、OpenAPI 配置、Redis 告警状态写失败保留待重试数据。
5. 前端检查：`app.js`、`design-lab.js` 语法通过，控制台契约检查和 Chrome 无头预览通过。
6. 工程检查：生产配置秘密扫描和 `src/docs` 下 `737` 个文件 UTF-8 严格解码通过。
7. 限制：真实 Redis、TDengine 和云平台进程级故障注入未执行，单元级故障模拟不得替代集成环境验收。

## 9. 2026-07-15 启动回归补充

1. `ApplicationContextTest` 使用随机端口启动完整 Spring Boot Web 上下文，覆盖 Tomcat、调度器、处理器和控制器装配。
2. `CollectedDataProcessorContextTest` 固定生产构造器选择，防止多构造器场景再次退化为查找无参构造器。
3. 默认测试启动使用文件配置、本地缓存并关闭 TDengine 和 MQTT，避免外部服务状态掩盖 Spring Bean 装配问题。

## 10. 2026-07-17 Modbus TCP 数据链路验收

1. 新增 `ModbusTcpCollectionChainAcceptanceTest`，通过真实 TCP Socket 和标准 Modbus TCP 报文连接 PLC4X 采集器，不使用采集器内部桩返回。
2. 验收范围为保持寄存器 `4001` 到 `4010`，确认连续点位合并为单次读取请求，两轮自增值、单点写回、缩放和偏移均正确。
3. 验证质量处理与告警元数据能够生成，缓存、实时流、历史和上报四类后处理阶段类型均收到相同的本地 `deviceId`。
4. 测试设备同时配置了不同的云端 `productKey/deviceName`，后处理链仍保持本地设备 ID，证明云身份不会反向覆盖控制台查询主键。
5. P0 固定入口 `mvn -q -Pp0-regression test`：`47` 项通过，失败 `0`、错误 `0`、跳过 `0`。
6. 全量入口 `mvn -q test`：`482` 项通过，失败 `0`、错误 `0`、跳过 `0`。
7. 当前机器未检测到 `502/1502/5020` 端口上的外部 Slave，因此没有把外部进程验收标记为通过。Slave 启动后执行：

```powershell
.\scripts\run-modbus-real-acceptance.ps1 -ServerHost 127.0.0.1 -Port 502 -UnitId 1 -StartRegister 4001 -PointCount 10
```

8. Slave 显示的保持寄存器 `4001` 到 `4010`，控制台点位地址应填写为 `44001` 到 `44010`；直接填写 `4001` 不包含寄存器区类型。
