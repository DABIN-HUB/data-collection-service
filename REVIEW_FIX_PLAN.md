# 线程/内存/流程风险修复方案

本文档用于记录本轮审查后准备落地的修复方案，范围聚焦以下 5 个问题：

1. `deferred=true` 上报结果无限重试，离线设备会持续堆积重试任务。
2. 异步后处理代际保护失效，旧设备代结果仍可能写缓存、上报、流和历史。
3. 多级缓存按 key 加锁实现存在竞态，同 key 访问不能保证串行。
4. 心跳监测会把每个连接都排进一个小线程池，网络抖动时容易积压。
5. 后处理线程池满时使用 `CallerRunsPolicy`，会把 I/O 写回压到采集主链路。
