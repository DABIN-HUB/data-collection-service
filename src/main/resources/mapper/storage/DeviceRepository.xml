<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wangbin.collector.storage.repository.DeviceRepository">

    <update id="createChildTable" statementType="STATEMENT">
        CREATE TABLE IF NOT EXISTS ${database}.${subTable}
        USING ${database}.${superTable}
        TAGS ('${deviceTag}', '${protocolTag}')
    </update>
</mapper>
