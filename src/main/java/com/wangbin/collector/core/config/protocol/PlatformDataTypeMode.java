package com.wangbin.collector.core.config.protocol;

/**
 * Describes how the platform-level dataType should be handled in the UI.
 */
public enum PlatformDataTypeMode {
    REQUIRED,
    DERIVED_EDITABLE,
    DERIVED_READONLY,
    ADVANCED
}